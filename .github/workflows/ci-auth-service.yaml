name: Bloom CI — auth-service

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'services/auth-service/**'
      - '.github/workflows/ci-auth-service.yaml'

permissions:
  contents: read

concurrency:
  group: ci-auth-service-${{ github.ref }}
  cancel-in-progress: true

env:
  MVN: "mvn -B --no-transfer-progress"

jobs:

  level1-compile:
    name: "[auth-service] Niveau 1 — Compilation"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven
      - name: Compile
        run: $MVN clean compile
        working-directory: services/auth-service

  level2-tests:
    name: "[auth-service] Niveau 2 — Tests"
    needs: level1-compile
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven
      - name: Tests
        run: $MVN test
        working-directory: services/auth-service
        env:
          SPRING_CLOUD_CONFIG_FAIL_FAST: 'false'
          SPRING_CLOUD_CONFIG_ENABLED: 'false'
          SPRING_CLOUD_CONFIG_IMPORT_CHECK_ENABLED: 'false'

      - name: Upload surefire reports (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports-auth-service
          path: services/auth-service/target/surefire-reports/

  level3-build:
    name: "[auth-service] Niveau 3 — Build final"
    needs: level2-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven
      - name: Vérification version SNAPSHOT
        run: |
          VERSION=$($MVN help:evaluate -Dexpression=project.version -q -DforceStdout)
          if [[ "$VERSION" != *-SNAPSHOT ]]; then
            echo "La version '$VERSION' doit être un SNAPSHOT sur une PR"
            exit 1
          fi
        working-directory: services/auth-service
      - name: Build final
        run: $MVN verify -DskipTests
        working-directory: services/auth-service
