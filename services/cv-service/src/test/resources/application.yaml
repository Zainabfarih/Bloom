spring:
  application:
    name: cv-service
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
