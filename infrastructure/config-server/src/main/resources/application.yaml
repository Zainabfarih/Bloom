server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ENSIAS-MEH/development-platform-bloom
          default-label: main
          search-paths: config-repo
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_TOKEN}

